// MODULE
var angularApp = angular.module('angularApp', []);

// CONTROLLERS will be the model , code inside here , we can pass it as an custom ng attribute  = controller  to any element , angular will look for controller inside myApp with that custom ng name 'mainController'

/// any code inside the element that contains the ng-controller will be associated eith controlling the html inside of that element


//controllers will be the models
angularApp.controller('mainController', ['$scope', function ($scope) {
    
}]);




